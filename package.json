{
  "name": "kenneth-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "dev": "node scripts/generate.mjs && vite",
    "build": "node scripts/generate.mjs && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "typescript": "^5.3.3",
    "vite": "^5.0.8"
  }
}
